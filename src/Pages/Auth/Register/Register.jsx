import classNames from "classnames/bind";
import styles from "./Register.module.scss";

const cn = classNames.bind(styles);
function Register() {
    return (
      <p1>Register Pages</p1>
    );
}

export default Register;
